import React from "react";

const OngoingProjects = () => {
  return (
    <div className="bg-[var(--bg-primary)] p-6 border border-[var(--border-color)] rounded-lg">
      <h3 className="text-[var(--text-primary)] font-semibold text-lg mb-4">Ongoing Projects</h3>
      <p className="text-[var(--text-muted)] text-sm">No ongoing projects to display at this time.</p>
    </div>
  );
};

export default OngoingProjects;
